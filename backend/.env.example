# Server
PORT=4000
FRONTEND_ORIGIN=http://localhost:5173

# MongoDB
MONGODB_URI=mongodb://127.0.0.1:27017/interview_system

# JWT
JWT_SECRET=change-me
JWT_EXPIRES_IN=7d

# Admin seed
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123

# SMTP (optional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=

# Feature toggles
EMAIL_ON_ONBOARD=false
EMAIL_ON_EVENT=false
EMAIL_ON_PAIRING=false

# Meeting + feedback
# Duration (minutes) used to decide when feedback opens (scheduledAt + MEETING_DURATION_MIN)
MEETING_DURATION_MIN=30

# Supabase (for event templates)
SUPABASE_URL=
# Prefer service key for server-side uploads; will also accept SUPABASE_KEY or SUPABASE_ANON_KEY
SUPABASE_SERVICE_KEY=
SUPABASE_BUCKET=templates
SUPABASE_PUBLIC=true
SUPABASE_SIGNED_TTL=600

# Supabase (for event templates)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
SUPABASE_BUCKET=templates
